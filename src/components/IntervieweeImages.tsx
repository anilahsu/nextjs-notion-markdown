import { IPost } from "@/lib/caseStudyType";

const IntervieweeImages = ({ context }: { context: IPost }) => {
  return (
    <>
      <p>IntervieweeImages</p>
    </>
  );
};

export default IntervieweeImages;
